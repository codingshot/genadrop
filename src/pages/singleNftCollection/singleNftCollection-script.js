export const getSort = ({ collections, date }) => {
  const dateToSecs = date * 60 * 60 * 24;

  // const filter = collection.filter((c) => c.createdAt >= dateToSecs);
  // console.log({collections});
};
